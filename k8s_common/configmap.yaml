apiVersion: v1
kind: ConfigMap
metadata:
  name: minishop-config
  namespace: minishop
data:
  DB_HOST: "postgres-external"
  DB_NAME: "minishop_db"
  DB_USER: "postgres"
  REDIS_HOST: "redis-external"
  JWT_SECRET: "minishop-secret-key"
