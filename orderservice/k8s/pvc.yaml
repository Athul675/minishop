apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orderservice-pvc
  namespace: minishop
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 1Gi
