apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: product-service-pvc
  namespace: minishop
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 1Gi
